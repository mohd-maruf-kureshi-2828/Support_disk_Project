**Mandatory AI Usage Report**

**AI TOOL USED**: Claude

**WHAT I ASKED AI TO DO**:

Help me choose one of the five assignments based on my background (Python/Django, and grievance and escalation handling at Teleperformance on the Flipkart process). Claude suggested Assignment 4 (Student Support & Ticket Management).
Design and generate the Django project: models, SLA and escalation logic, views, templates, demo seed data.
Write the README (approach, assumptions, trade-offs, edge cases).

**PROMPT THAT WAS MOST USEFUL**: "Choose the best assignment for me and build the complete working project. I am a beginner, please help me build everything." Claude then chose Assignment 4 and generated the full Django project and documentation.

**CODE GENERATED BY AI**: Most of the initial project: models (Category, Ticket, TicketActivity), services.py (SLA calculation, auto-assignment, status rules, escalation), views, HTML templates, forms, admin, and the seed_demo / check_sla management commands.

**CODE I MODIFIED**: I did not need to change the application logic after running it. My work was setting up the environment, running migrations and the demo data, running and testing the app, creating the GitHub repository and preparing the submission.

**AI OUTPUT THAT WAS WRONG**: The AI could not run Django in its own sandbox (no internet), so it only syntax-checked the code and could not run it. It told me this clearly and said errors might appear when I run it. When I ran it on my laptop, the project started successfully, so I did not hit a real bug in the generated code.

**HOW I IDENTIFIED THE PROBLEM**: I did not find a functional bug. I verified the code by running the server and using the app in the browser. I treated the AI's "not tested" warning as a reason to test everything myself instead of trusting it.

**HOW I FIXED IT**: No fix was needed for the generated code. The only risk (untested code) was handled by running the full setup locally: makemigrations, migrate, seed_demo, runserver.

**HOW I VALIDATED THE OUTPUT**: I ran the project locally and checked it in the browser with the demo users (manager, staff, student). I checked the ticket flow (create ticket, auto-assignment, status changes) and the dashboard, and I read through the README so I can explain the SLA pause and escalation logic.